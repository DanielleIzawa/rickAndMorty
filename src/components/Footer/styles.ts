import styled from "styled-components";

import Theme from "../../commom/styles/theme";

export const Container = styled.div`
  height: 60px;
  width: 100%;
  position: fixed;
  bottom: 0;
  background-color: ${Theme.colors.black_container};
`;
